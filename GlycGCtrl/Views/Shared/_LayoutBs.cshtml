<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <title>@ViewBag.Title - Glycemic Graphic Control</title>
        <link href="~/favicon.ico" rel="shortcut icon" type="image/x-icon" />
        <meta name="viewport" content="width=device-width" />

        @Styles.Render("~/Content/bootstrap/css")
        @Styles.Render("~/Content/SiteBs.css")
        @Scripts.Render("~/bundles/modernizr")
    </head>
    <body>
        <header>
            <div class="container">
                <div class="row">
                    <div class="span3">
                        <h3>@Html.ActionLink("your logo here", "Index", "Home")</h3>
                    </div>
                    <div class="offset5 span4">
                        <section id="login" style="margin:10px 0px">
                            @Html.Partial("_LoginPartialBs")
                        </section>
                    </div>
                </div>
                <div class="row">
                    <nav>
                        <ul id="menu" class="nav nav-pills pull-right">
                            @Html.LiLink("Home", "Index", "Home")
                            @Html.LiLink("About", "About", "Home")
                            @Html.LiLink("Contact", "Contact", "Home")
                        </ul>
                    </nav>
                </div>
            </div>
        </header>

        <div id="body" class="container">
            <div class="row">
            @RenderSection("featured", required: false)
            <section class="content-wrapper main-content clear-fix">
                @RenderBody()
            </section>
            </div>
        </div>

        @Scripts.Render("~/bundles/jquery")
        @Scripts.Render("~/bundles/bootstrap")
        @RenderSection("scripts", required: false)
    </body>
</html>